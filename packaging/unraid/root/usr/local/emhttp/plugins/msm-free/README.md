# msm-free

`msm-free` is an open reimplementation of the MSM-style MosDNS + Mihomo manager.

The Unraid plugin stores persistent data under `/mnt/user/appdata/msm-free` by default and exposes a settings page under **Settings / MSM Free**.
